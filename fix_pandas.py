# วิธีแก้ไขไฟล์ app.py โดยไม่ใช้ pandas

# 1. แทนที่ทุก function ที่ใช้ pandas ด้วย CSV module
# 2. ใช้ built-in csv และ io modules
# 3. ลบ pandas import ทั้งหมด

# สร้างไฟล์ app.py ใหม่โดยไม่ใช้ pandas
